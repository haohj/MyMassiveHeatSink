# Mod Config Directory

这个目录用于存放本 Mod 的配置文件。

建议文件名：

- `config.json`

示例内容：

```json
{
  "ExhaustKilowattsWhenActive": -16.0,
  "minimumTemperature": 100.0
}
```

说明：

- 兼容旧键：`minimumTemperature`
- 代码中的规范属性名：`MinimumTemperatureC`

